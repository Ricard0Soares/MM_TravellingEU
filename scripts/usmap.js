var simplemaps_europemap = {
    mapdata: simplemaps_europemap_mapdata,
    load: function() {
        // Inicialize o mapa usando o mapdata
        console.log("Carregando mapa com os seguintes dados:", this.mapdata);
        // Lógica de inicialização do mapa vai aqui
        // Isso pode incluir a criação do mapa, renderização no div `map1`, etc.
    }
};

// Chame a função de inicialização
simplemaps_europemap.load();
